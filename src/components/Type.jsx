// eslint-disable-next-line react/prop-types
export default function CommType({ type }) {
  return (
    <li>
      {/* eslint-disable-next-line react/prop-types */}
      <p>{type.name}</p>
    </li>
  );
}
